import type { AuthObject } from "../utils/public";
import type { SetClaim } from "./models";
type ClaimKind = "completed" | "dropped" | "expired";
export declare const getClaims: (authorization: AuthObject, payload: {
    claimKind: ClaimKind;
}) => Promise<SetClaim[]>;
export {};
